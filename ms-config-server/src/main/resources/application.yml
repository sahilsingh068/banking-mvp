server:
  port: 8082
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sahilsingh068/microservice-tutorial-config
          clone-on-start: true
